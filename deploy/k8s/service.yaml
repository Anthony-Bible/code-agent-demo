apiVersion: v1
kind: Service
metadata:
  name: code-agent-demo
  namespace: code-agent
  labels:
    app.kubernetes.io/name: code-agent-demo
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: code-agent-demo
