global:
  resolve_timeout: 5m

route:
  receiver: code-agent
  group_wait: 5s
  group_interval: 10s
  repeat_interval: 1m

receivers:
  - name: code-agent
    webhook_configs:
      - url: "http://code-agent:8080/alerts/prometheus"
        send_resolved: true
